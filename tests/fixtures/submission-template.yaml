# Coldline - Task 2.9
# File: submission.yaml
# Component: Student answer sheet
# Purpose: Record the two prior decisions this defense reuses.
# Interacts With: Submission schema, public verifier, sprint review checks
# Sprint/Task: Sprint 2 - Project 2 / Task 2.9
# Concepts: Evidence reuse, decisions embodied in code
# Tools: YAML

# Complete submission.yaml from the evidence sources named beside each answer group.
# Keep the supplied keys; replace empty strings using each field's stated type.
# Empty values and placeholder text fail verification.
# Use submission-sample.yaml for format only; do not copy its fictional values.
# Do not include secrets, personal data, or local absolute paths.

answers:
  # Task 2.9 — Step 1: Instructor Presentation / Review
  # Assemble pull request evidence across Sprint 2.
  # Record the boundary and experiment decisions embodied in this repository.
  # Run `poe review-evidence` for the files to inspect. Your own choices in the
  # Task 2.2 and Task 2.7 PRs remain evidence for the live defense in Steps 2–3.
  # Source: docs/student/task-2-9-contract.md, "The two answers, and where they come from".
  # The defense and the instructor's completion record are outside this answer sheet.
  #
  # Boundary options, originating in Task 2.2 (use exact strings):
  # - retrieval_orchestration: Build the retrieval request, invoke the Retriever
  #   port, and select candidates to send to the next stage.
  # - context_assembly: Apply the token budget, format the prompt, and build citations.
  #
  # Choose one boundary above delegated to a service by this repository's composition.
  # Inspect src/api/retrieval_workflow.py and the wiring named by `poe review-evidence`.
  defended_boundary: ""

  # Experiment decision options, originating in Task 2.7 (use exact strings):
  # - keep: Retain the experimental retrieval configuration.
  # - revert: Restore the baseline retrieval configuration.
  #
  # Choose one decision above reflected by config/student/retrieval.yaml compared
  # with config/retrieval-baseline.yaml in this repository.
  defended_decision: ""
